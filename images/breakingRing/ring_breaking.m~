function ring_breaking
    addpath('../../matlab/common');
    addpath('../../matlab/product_3');
    
    type = 2;
    tau = 0.5;
    number = 5;
    epsilon = 0.05;   
    dir = '';
    ext = 'png';
    
     [minX, maxX, figure1] = plotterBoundness(type, tau, number, epsilon, dir, c);
     
 end