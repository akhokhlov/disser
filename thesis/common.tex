% Общие поля титульного листа диссертации и автореферата
\institution{ЮЖНО-УРАЛЬСКИЙ ГОСУДАРСТВЕННЫЙ УНИВЕРСИТЕТ}

\topic{УСТОЙЧИВОСТЬ НЕЙРОННЫХ СЕТЕЙ\\ ХОПФИЛДА С ЗАПАЗДЫВАНИЕМ}

\author{Хохлова Татьяна Наилевна}

\specnum{05.13.18}
\spec{математическое моделирование, численные методы и комплексы программ}
%\specsndnum{01.04.07}
%\specsnd{Физика конденсированного состояния}


\sa{М.\,М.~Кипнис} \sastatus{д.~ф.-м.~н., проф.}
%\sasnd{ФИО второго руководителя}
%\sasndstatus{к.~ф.-м.~н., проф.}

%\scon{ФИО консультанта}
%\sconstatus{д.~ф.-м.~н., проф.}

\city{Челябинск}
\date{\number\year}

% Общие разделы автореферата и диссертации
\mkcommonsect{actuality}{Актуальность работы.}{%
Текст об актуальности \cite{Matsunaga_2009}.
Ссылка~\cite{Idels&Kipnis_2009,Azbelev_1997,Dreissche_1998,Kiryanen_1994}
}

\mkcommonsect{objective}{Цель диссертационной работы.}{%
Основным объектом диссертационнго исследования является уравнение
\begin{equation}\label{common:maineqmatrix}
    \dot{x}(t)+A x(t)+B x(t-\tau)=0, 
\end{equation}
где~$n\times n$ матрица~$A$ есть матрица мгновенных взаимодействий, а~$n\times n$ 
матрица~$B$~--- матрица взаимодействий с~запаздыванием.  

Для достижения поставленных целей были решены следующие задачи: \ldots

}

\mkcommonsect{novelty}{Научная новизна.}{%
Текст о новизне.
}

\mkcommonsect{value}{Практическая значимость.}{%
Результаты, изложенные в диссертации, могут быть использованы для \ldots
}

\mkcommonsect{results}{%
На защиту выносятся следующие основные результаты и положения:}{%
Текст о результатах.
}

\mkcommonsect{approbation}{Апробация работы.}{%
Основные результаты, изложенные в диссертации, докладывались на седьмой
Всероссийской конференции с международным участием <<Математическое
моделирование и краевые задачи>> (Самара, 2010г.), второй и четвёртой научных
конференциях аспирантов и докторантов (Челябинск, 2010г. и 2012г.), Всероссийской
конференции <<Статистика. Моделирование. Оптимизация>> (Челябинск, 2011г.), II
Международной научно-практической конференции студентов и аспирантов
<<Математика и её приложения в современной науке и практике>> (Курск, 2012г.),
на семинаре профессора М.~М.~Кипниса в Челябинском государственном
педагогическом университете.

}

\mkcommonsect{pub}{Публикации.}{%
Материалы диссертации опубликованы в $12$ печатных работах, 
из них $3$ статьи в рецензируемых журналах
\citemy{Khokhlova_2010,Khokhlova&Kipnis&Malygina_2011,Khokhlova&Kipnis_2012}, 
$6$ статей в сборниках трудов конференций
\citemy{conf_Samara_2010,conf_SUSU_2010,conf_SMO_2011,conf_SMO2_2011,conf_Kursk_2012,conf_SUSU_2012},
и $3$ комплекса программ зарегистрировано в~Объединенном фонде электронных
ресурсов <<Наука и образование>> ~\citemy{product1_ofernio,product2_ofernio,product3_ofernio} 
(сами программы доступны в~интернете~\citemy{program1,program2, program3}).
 %и $n_3$ тезисов докладов. 
}

\mkcommonsect{contrib}{Личный вклад автора.}{%
Содержание диссертации и основные положения, выносимые на защиту, отражают
персональный вклад автора в опубликованные работы. Подготовка к публикации
полученных результатов проводилась  совместно с соавторами, причём вклад
диссертанта  был определяющим. Все представленные в диссертации результаты
получены лично автором. }

\mkcommonsect{struct}{Структура и краткое содержание диссертации.}{%
Диссертация состоит из введения, четырех глав, заключения, списка литературы и
четырех приложений.
{\bfseries Во введении} приводится постановка задачи, история исследуемого
вопроса, ставятся цели диссертационнго исследования, обосновывается актуальность и научная
новизна работы. Кроме того, приводятся публикации по теме диссертации,
описывается структура работы и ее краткое содержание.

 {\bfseries Первая глава} посвящена описанию и обоснованию метода конусов
устойчивости для диагностирования устойчивости скалярного аналога уравнения
\eqref{common:maineqmatrix} и собственно уравнения \eqref{common:maineqmatrix} с
совместно триангулируемыми матрицами $A, B$.
%Метод разработан автором диссертации под началом научного руководителя.

В разделе \ref{sec11} приведены актуальные на сегодняшний день модели,
описывающие взаимодействие нейронов в нейронных сетях с помощью матричных дифференциальных
уравнений, обосновано введение запаздывания и показано, что во многих случаях
исследование устойчивости различных моделей нейронных сетей сводится к изучению
поведения решений уравнения \eqref{common:maineqmatrix}. Также в этом разделе
приведены примеры нейронных сетей, описываемых
уравнением \eqref{common:maineqmatrix}, отмечены особенности входящих в
уравнение матриц $A, B$, элементами которых служат интенсивности
мгновенного взаимодействия нейронов в сети и взаимодействия, происходящего с
запаздыванием, соответственно.

В~этой главе конус устойчивости вводится постепенно. Вначале
(раздел~\ref{sec12}) дается определение устойчивости и вводятся овалы
устойчивости для~скалярного уравнения вида~\eqref{common:maineqmatrix}
\begin{equation}\label{common:scalar} 
    \dot x\left(t\right) + a\, x(t) +  b\, x(t-\tau) = 0,\quad
\tau > 0.
\end{equation} 
с~действительным коэффициентом~$a$ и комплексным~$b$. Здесь же
формулируется и с помощью метода
${D}$\,-разбиений~\cite{Gryazina&Polyak_2006} доказывается теорема об
устойчивости такого уравнения в терминах овала устойчивости.
 
Затем, в разделе~\ref{sec13}, дается ключевое определение конуса устойчивости
для~скалярного уравнения \eqref{common:scalar} с~комплексными коэффициентами~$a,
b$, которое будет~использоваться также и для~матричного
уравнения~\eqref{common:maineqmatrix}.

\begin{determine}\label{common:defin_cone}
Конусом устойчивости для уравнений \eqref{common:scalar},
\eqref{common:maineqmatrix} назовём множество точек $M=(u_1,u_2,u_3)\in \mathbb{R}^3$, 
таких, что
\begin{equation}\label{common:cone}
    \left\{
        \begin{array}{l}
            u_1=-h \cos{\omega} + \omega \sin{\omega},\\
            u_2=h \sin{\omega} + \omega \cos{\omega},\\
            u_3 = h,
        \end{array}
    \right.
\end{equation}
где действительные параметры~$h, \omega$ подчинены ограничениям
\begin{equation}\label{common:preimage}
    \left\{
        \begin{array}{l}
            h\geqslant -\dfrac{\omega}{\tg{\omega}},\\[2mm]
            -\pi<\omega<\pi .\\
        \end{array}
    \right.
\end{equation}
\end{determine}

В терминах конуса устойчивости формулируются и естественным образом доказываются
теоремы об устойчивости уравнения \eqref{common:scalar} с~действительным
коэффициентом~$a$ и комплексным~$b$, а затем с~двумя комплексными коэффициентами~$a,
b$. 

В~основном разделе~\ref{sec14} доказывается теорема о~конусе  
устойчивости для матричного уравнения~\eqref{common:maineqmatrix}, которая будет 
теоретической основой для~алгоритмов диагностирования устойчивости нейронных сетей 
и их программных реализаций. 

\begin{theorem}\label{common:th:stab1}
Пусть $A, B, S\in \mathbb{R}^{m\times m}$ и $S^{-1}AS=A_{T}$ и $S^{-1}BS=B_{T}$,
где $A_{T}$ и $B_{T}$~--- нижние треугольные матрицы с~элементами соответственно 
$\lambda_{j\,s}, \mu_{j\,s}\,(1 \leqslant  j,\, s \leqslant  m)$. 
Построим систему точек 
$M_j=(u_{1j},u_{2j},u_{3j})$, $(1\leqslant j \leqslant m)$,
так, что
\begin{equation}\label{common:pointsM}
    \begin{array}{l}
        u_{1j} = \tau \Re ( \mu_{jj} \, \exp({i\tau \Im \lambda_{jj}})),  \\
        u_{2j} = \tau \Im ( \mu_{jj} \, \exp({i\tau \Im \lambda_{jj}})),  \\
        u_{3j} = \tau \Re \lambda_{jj}.
    \end{array}
\end{equation}
Уравнение~\eqref{common:maineqmatrix} асимптотически устойчиво тогда и
только тогда, когда все точки $M_j$ $(1\leqslant j \leqslant m)$ находятся внутри 
конуса устойчивости~\ref{common:defin_cone}. Если хотя бы одна точка 
$M_j$  $(1\leqslant j \leqslant m)$ лежит вне~конуса устойчивости, то
уравнение~\eqref{common:maineqmatrix} неустойчиво.
\end{theorem}

Эта теорема является главным результатом первой главы и дает возможность
исследования устойчивости широкого класса уравнений вида~\eqref{common:maineqmatrix} 
с совместно триангулируемыми матрицами произвольного порядка.

В этом же разделе получено необходимое и достаточное условие на собственные
числа матриц $A, B$, обеспечивающее устойчивость основного уравнения \eqref{common:maineqmatrix} при
всех значениях запаздывания:

\begin{equation}\label{common:dis_condition}
     \forall j \; (1 \leqslant  j \leqslant  n) \quad  |\mu_{jj}| < \Re
    \lambda_{jj}.
\end{equation}

Условие \eqref{common:dis_condition} отражено в отдельной теореме и представляет
практический интерес при работе с конкретными нейронными сетями и возможности 
управления коэффициентами системы.

В разделах~\ref{sec13}, ~\ref{sec14} также приведены примеры применения
полученных критериев для анализа устойчивости уравнений \eqref{common:scalar},
\eqref{common:maineqmatrix}, примеры снабжены графическим представлением конуса
устойчивости и точек $M_j$, фигурирующих в формулировке основной теоремы
\eqref{common:th:stab1}.

Особняком стоит раздел~\ref{sec15} о~системе неравенств 
для~диагностики устойчивости уравнения~\eqref{common:scalar} с~комплексными
коэффициентами. Он не~будет использоваться далее в~диссертации и содержит
зависящие от коэффициентов условия устойчивости исследуемого уравнения. Однако
эти условия излишне громоздки и лишены наглядной геометрической интерпретации, 
в связи с чем для дальнейшей работы с матричным
уравнением~\eqref{common:maineqmatrix} и конкретными нейронными сетями,
описываемыми этим уравнением, автором был сделан выбор в пользу метода конусов
устойчивости.


В разделе \ref{sec16} результаты первой главы сравниваются с известными
результатами.

 {\bfseries Вторая глава} посвящена алгоритму
определения значений запаздываний, гарантирующих устойчивость 
уравнения~\eqref{common:maineqmatrix} при
фиксированных матрицах $A, B$, и его программной реализации. Алгоритм 
основан на методе конусов устойчивости (теорема \ref{common:th:stab1}), но при
этом он содержит только аналитические выкладки и позволяет избежать
геометрических построений при анализе устойчивости рассматриваемого уравнения.
Алгоритм подробно описан и обоснован в разделе \ref{sec21} в рамках следующей за
ним теоремы.

Пусть матрицы $A$ и $B$ приведены к треугольному виду $A_T$, $B_T$ одним 
преобразованием. Пусть $\lambda_{j\,j}, \mu_{j\,j},\; 1 \leqslant  j \leqslant n$  
собственные числа матриц $A$ и $B$ в~том порядке, в котором они расположены на
диагоналях  матриц $A_T$, $B_T$. Для каждого $j,\; 1 \leqslant  j \leqslant  n$
составим множество $T_{j}$ по следующему алгоритму. 

Если для некоторого $j$ выполнено $\Re{\lambda_{j\,j}} > |\mu_{j\,j}|$, то
$T_{j}=(0, +\infty)$. В~противном случае находим числа 
\begin{multline}
\label{tau} \tau_{j}^{m}=\frac{\arg{(\pm iQ - \Re{\lambda_{j\,j}})}-\arg
{\mu_{j\,j}} +2\pi m}{\Im{\lambda_{j\,j}\pm Q}}, \quad m\in \mathbb{Z}, \\ 
Q=\sqrt{|\mu_{j\,j}|^{2}-(\Re{\lambda_{j\,j}})^{2}}.
\end{multline}

\noindent
Из чисел $\tau_{j}^{m}$ выбираем такие, для которых

\begin{equation}
\label{inequality_tau} 0<\tau_{j}^{m}\leq\pi/Q  \quad \mbox{и} \quad 
\Re{\lambda_{j\,j}} \geqslant -\frac{Q}{\tg(Q\,\tau_{j}^{m})},
\end{equation}
и сортируем по возрастанию, исключая повторения. Полученные числа обозначим
$\tau_{1 j}, \tau_{2 j}, \ldots ,\tau_{s j}$. 

\noindent
Составим множество $T_{j}$ следующим образом. Пусть
$
 T_{j}=(0,\tau_{1 j})\cup(\tau_{2 j}, \tau_{3 j})\cup\ldots
$
в следующих случаях:

1) $\Re{\lambda_{j j}} > 0$ и выполнено хотя бы одно условие

\begin{equation}
\label{Re>0}
 \mbox{а)}\; \arg {\mu_{j\,j}}\in(-\pi/2, \pi/2)   \quad  \mbox{или} \quad   
 \mbox{б)}\; \frac{\Re{\lambda_{j j}}}{|\Re{\mu_{j j}}|} > 1.
\end{equation}


\begin{equation}
\label{Re<0}
2)  \Re{\lambda_{j j}} < 0, \quad 
\left|\frac{\Re{\lambda_{j j}}}{\Re{\mu_{j j}}}\right| < 1 \quad 
\mbox{и} \quad 
\arg {\mu_{j\,j}}\in(-\pi/2, \pi/2).
\end{equation}

\noindent В остальных случаях
$
 T_{j}=(\tau_{1 j}, \tau_{2 j})\cup(\tau_{3 j}, \tau_{4 j})\cup\ldots
$

\noindent
Последним интервалом в $T_{j}$ может быть $(\tau_{s-1 j}, \tau_{s j})$ или
$(\tau_{s j}, +\infty)$. Если для некоторого $j$ множество $(\tau_{1j}, 
\tau_{2 j}, \ldots ,\tau_{s j})$ пусто и выполнено \eqref{Re>0} или
\eqref{Re<0}, то $T_{j}=(0, +\infty)$.   

\begin{theorem} \label{common:algorithm_th} Пусть множества $T_{j}, 1 \leqslant
j \leqslant  n$ составлены согласно описанному алгоритму. 
Тогда уравнение~\eqref{common:maineqmatrix}
асимптотически устойчиво если и только если  $\tau \in T=\bigcap_{j} T_{j}$. 
\end{theorem}

Данный алгоритм дает возможность исследовать устойчивость различных уравнений
вида~\eqref{common:maineqmatrix} аналитически, что привело автора к созданию
реализующего его программного продукта. Программа <<Анализ устойчивости>>,
разработанная в~программном пакете 
\Eng MATLAB~7.11.0~(R2010b) \Russian, описана в разделе \ref{sec22}. По
заданным матрицам $A, B$ программа возвращает объединение интервалов значений
запаздывания $T$, обеспечивающих устойчивость
уравнения~\eqref{common:maineqmatrix}, или пустое множество  в случае
неустойчивости уравнения при всех значениях запаздывания. Здесь же приводятся
скриншоты интерфейса программы, примеры двух способов введения исходных данных, 
необходимые пояснения к работе программы.





{\bfseries В третьей главе} решается задача диагностирования устойчивости
кольцевых и линейных нейронных сетей с неограниченным количеством нейронов с
помощью результатов первой и второй глав диссертации, а также выводятся
новые теоретические результаты для анализа устойчивости таких сетей в
зависимости от коэффициентов моделей.

В разделе \ref{sec31} вводятся две модели кольцевых нейронных сетей, являющиеся
основным объектом изучения третьей главы.

\begin{equation}\label{common:main1}
    \dot{x}_{j}(t) +  x_{j}(t) +  a\, x_{j-1}(t) + b\, x_{j+1}(t-\tau) = 0 \qquad 
    (j\; {\rm{mod}}\; n),
\end{equation}
\begin{equation}\label{common:main2}
    \dot{x}_{j}(t) +  x_{j}(t) +  a\, x_{j-1}(t-\tau) + b\, x_{j+1}(t-\tau) = 0
    \qquad  (j\; {\rm{mod}}\; n).
\end{equation}

Уравнение~\eqref{common:main1} соответствует малым запаздываниям
взаимодействия нейронов  с правыми соседними нейронами (сети с односторонним
запаздыванием), а~\eqref{common:main2} --- близким запаздываниям взаимодействия
нейронов с правыми и левыми соседями  (сети с двусторонним
запаздыванием). Данные уравнения принадлежат классу систем вида
~\eqref{common:maineqmatrix} и могут быть исследованы с помощью разработанных
автором методов.

В этом же разделе описана модификация алгоритма диагностирования устойчивости
уравнений вида~\eqref{common:maineqmatrix}
для случая специфических матриц, входящих в модели кольцевых сетей, и
неограниченного количества нейронов в сети. Суть изменений заключается в
явном нахождении собствнных чисел входящих в уравнение матриц и введении вместо
дискретной системы точек~$M_j$ (\ref{common:pointsM})  непре\-рыв\-ной 
замкнутой кривой $M^{'}(t)=(u_1^{'}(t), u_2^{'}(t), u_3^{'}(t))$ для уравнения
~\eqref{common:main1}:
\begin{multline}\label{common:curvemain1}
    u_{1}^{'}(t)+iu_{2}^{'}(t) = \tau b \,\exp(i(-t+a\, \tau\sin t)),  \\
    u_{3}^{'}(t) = \tau (1+a\cos t),  \quad 0\leqslant t \leqslant 2\pi.                                  
\end{multline}
и $M^{''}(t)=(u_1^{''}(t), u_2^{''}(t), u_3^{''}(t))$ для уравнения
~\eqref{common:main2}:
\begin{equation}\label{common:curvemain2}
    u_{1}^{''}(t)+iu_{2}^{''}(t)  = \tau (a \,\exp(it) +b \,\exp(-it) ),  \quad
    u_{3}^{''}(t) = \tau,   \quad 0\leqslant t \leqslant 2\pi.                                    
\end{equation}

Для рассмотренных систем критерий устойчивости принимает следующий вид. Если все
точки кривой~\eqref{common:curvemain1} лежат внутри конуса устойчивости, 
то уравнение~\eqref{common:main1} устойчиво при любом $n$,  
а если хотя бы одна точка кривой \eqref{common:curvemain1} лежат вне конуса
устойчивости, то уравнение~\eqref{common:main1} неустойчиво при всех достаточно
больших значениях $n$. Аналогично изучается поведение системы
~\eqref{common:main2}. Идея критерия проиллюстрирована примером с рисунком.

В разделе \ref{sec32} описан программный продукт <<Устойчивость нейронных
сетей>>, предназначенный для исследования устойчивости систем 
\eqref{common:main1},  \eqref{common:main2} с неограниченным количеством нейронов. 
Продукт разработан автором на основе модификации алгоритма, описанной в разделе
\ref{sec31}, и позволяет быстро по введенным коэффициентам диагностировать
устойчивость конкретной нейронной сети и получить геометрическую интерпретацию
результатов. В этом разделе представлены скриншоты интерфейса программы с
примерами ее работы, необходимые пояснения.     

В разделе \ref{sec33} изложены результаты теоретического и численного
исследования границ областей устойчивости уравнений~\eqref{common:main1},
~\eqref{common:main2} в плоскости параметров $(a, b)$ в случае неограниченного
количества нейронов.

\begin{theorem}\label{common:th:stab_with_great_n}
    Если $|a+b|>1$, то  системы~\eqref{common:main1},
~\eqref{common:main2} неустойчивы при любом $\tau > 0$,
    если $n$ достаточно велико.
\end{theorem}

При помощи программы <<Устойчивость нейронных сетей>> для различных
значений запаздывания построены границы областей устойчивости $D_{\tau}$
уравнений~\eqref{common:main1}, ~\eqref{common:main2} в тех частях плоскости $(a, b)$, 
которые не охватываются теоремой \ref{common:th:stab_with_great_n} и ранее
известным фактом об устойчивости исследуемых моделей при  $|a|+|b|<1$ для любого
$n$ и любого $\tau\geqslant 0$. 
Полученные результаты отражены на приведенных рисунках.

В этом же разделе вводится определение границ устойчивости
системы~\eqref{common:main1}, ~\eqref{common:main2} при $a=-b$ для больших
$n$ и формулируется теорема об асимптотическом поведении введенных функций.
Здесь же приводятся результаты численного нахождения значений данных функций при
различных запаздываниях $\tau$, полученные с помощью программы <<Устойчивость
нейронных сетей>>.  

Доказательства теорем данного раздела будут приведены позже.

В разделе \ref{sec34} рассматривается вопрос устойчивости кольцевых нейронных
сетей с неединичным коэффициентом демпфирования на примере аналога
системы~\eqref{common:main1} с~$\gamma\in\mathbb{R}$:
\begin{equation}\label{common:eq_gamma}
    \dot{x}_{j}(t) +  \gamma\,x_{j}(t) +  a\, x_{j-1}(t) + b\, x_{j+1}(t-\tau) =
    0 \qquad (j\; {\rm{mod}}\; n).
\end{equation}

Указаны замена переменных, сводящая это уравнение к уравнению
вида~\eqref{common:main1}, и способы диагностирования устойчивости
уравнения~\eqref{common:eq_gamma} при ограниченном и неограниченном количестве
нейронов. Приведены границы областей устойчивости
уравнений~\eqref{common:eq_gamma},~\eqref{common:main1} в плоскости параметров $(a, b)$, структура которых не 
содержит принципиальных различий. Данный раздел призван продемонстрировать целесообразность 
рассмотрения именно
уравнения~\eqref{common:main1} с единичным коэффициентом при~$x_{j}(t)$ и
возможность применения полученных для него результатов к некоторым более общим
уравнениям. 

Раздел \ref{sec35} посвящен устойчивости нейронных сетей линейной конфигурации,
взаимодействие в которых описывается уравнением~\eqref{common:maineqmatrix} с
матрицами специального вида.   

Основным объектом изучения данного раздела является уравнение:
\begin{equation}\label{common:matrixbrokenequ}
\dot{x}(t) +I\,x(t)+ D\,x(t-\tau)=0,    
\end{equation}
 где $n\times n$ матрица $D$ имеет вид
\begin{equation}\label{common:matrixbroken}
D=
\begin{pmatrix}  
  0  &  b & 0 &   \ldots &  0 &  0\\
  a  &  0 & b &   \ldots &  0 &  0\\
  0  &  a & 0 &   \ldots &  0 &  0\\
  \vdots  & \vdots & \vdots  &   \ddots &  \vdots &  \vdots \\
  0  &  0 & 0 &   \ldots &  0 &  b\\
  0  &  0 & 0 &   \ldots &  a &  0   \\
\end{pmatrix}.
\end{equation}

Уравнение \eqref{common:matrixbrokenequ},\eqref{common:matrixbroken} по сути
описывает кольцевую сеть~\eqref{common:main2} с двусторонним запаздыванием при
разрыве одной из связей между нейронами.   

Следующий результат дает возможность отвечать на вопрос об устойчивости системы 
\eqref{common:matrixbrokenequ},  \eqref{common:matrixbroken} при любых $a, b$.
Пусть функция $F(\tau)$ от запаздывания $\tau\in (0,\infty)$ определяется
следующим образом:  
\begin{equation}
\label{common:Ftau} 
F(\tau)= \frac{1}{4\sin^2\omega(\tau)},
\end{equation}
где $\omega(\tau)$ есть наименьший положительный корень уравнения 
\begin{equation}
\label{common:omegaboundary} 
\tau= \omega \tg \omega.
\end{equation}

Тогда справедлива теорема.
\begin{theorem}\label{common:th:stab_linear}
1. Если  $|ab|<\frac {1}{4} $, то система  \eqref{common:matrixbrokenequ},
\eqref{common:matrixbroken} асимптотически устойчива при любом $n$ и любом
$\tau\geqslant 0$.\\  
2. Если $ab> \frac {1}{4}$, то  система  \eqref{common:matrixbrokenequ},
\eqref{common:matrixbroken} неустойчива при любом $\tau\geqslant 0$, если $n$
достаточно велико. \\  
3. Если  $ab<0$ и $|ab|<F(\tau)$, то система \eqref{common:matrixbrokenequ},
\eqref{common:matrixbroken} асимптотически устойчива  при любом $n$.\\ 
4. Если $ab<0$ и  $|ab|>F(\tau)$, то система \eqref{common:matrixbrokenequ},
\eqref{common:matrixbroken} неустойчива, если $n$ достаточно велико.  
\end{theorem}

Раздел \ref{sec35} завершается рисунком, на котором в плоскости $(a, b)$
изображены границы областей устойчивости и неустойчивости системы
\eqref{common:matrixbrokenequ}, \eqref{common:matrixbroken}, полученные с
помощью теоремы \eqref{common:th:stab_linear} и численных экспериментов в
программе <<Устойчивость нейронных сетей>>. Вычисления потребовались для
построения границ, зависящих от значения запаздывания $\tau$. Проводится
сравнение полученных областей устойчивости и неустойчивости кольцевой сети
нейронов~\eqref{common:main2} и образованной при ее разрыве нейронной сети
линейной конфигурации \eqref{common:matrixbrokenequ},
\eqref{common:matrixbroken}.     

Раздел \ref{sec36} содержит доказательства теорем третьей главы. В разделе
\ref{sec37} приведено сравнение результатов данной главы с известными
результатами.

{\bfseries Четвертая глава} посвящена построению границ областей устойчивости
кольцевых и линейных нейронных сетей с ограниченным количеством нейронов в плоскости
параметров $(a, b)$. 

Основными объектами изучения данной главы служат
уравнения кольцевых нейронных сетей~\eqref{common:main1} и~\eqref{common:main2}
с ограниченным количеством нейронов.

Раздел \ref{sec41} посвящен описанию алгоритма и программы построения границ
областей устойчивости и неустойчивости исследуемых уравнений. Подраздел
\ref{subsec411} содержит общую схему построения замкнутой кривой, состоящей из
равномерного массива точек и соответствующей границе области устойчивости круговых нейронных
сетей~\eqref{common:main1} и~\eqref{common:main2}. Для осуществления этой схемы
требуется диагностировать большое количество точек плоскости  $(a, b)$ на
устойчивость при фиксированном значении запаздывания. В подразделе
\ref{subsec412} указан новый алгоритм, предназначенный для  этой цели, а также
подробно описана работа реализующей его функции  \emph{straightStabAnalizer}
(код функции см.~приложение~В).
Алгоритм решает частную задачу определения  устойчивости системы при данных $a, b, \tau$ в
отличие от более общей задачи определения всех значений запаздывания,
гарантирующих устойчивость исследуемого уравнения при фиксированных
коэффициентах, решенной во второй главе диссертации. В связи с этим данный
алгоритм выигрывает во времени и хорошо подходит для многократного применения в
общей схеме построения границы области устойчивости, изложенной в подразделе
\ref{subsec411}.

 В подразделе \ref{subsec413} указаны функциональное назначение и область
 применения программы <<Построение  областей устойчивости круговых нейронных
 сетей>>, реализующей алгоритмы подразделов \ref{subsec411},
\ref{subsec412}. Примеры использования программы, скриншоты ее работы
приведены в подразделе \ref{subsec414}.



В разделе \ref{sec42} представлены результаты построения границ областей
устойчивости нейронной сети~\eqref{common:main1}, полученные с помощью данной
программы. Приведенные серии графиков соответствуют различным значениям
запаздывания и различному количеству нейронов в сети. На графиках отмечена
граница области устойчивости, гарантированной для любого $n$, что позволяет
проследить асимптотику найденных кривых с увеличением количества нейронов в
сети.

 В разделе \ref{sec43} аналогичные построения проведены для
 модели~\eqref{common:main2}.

В разделе \ref{sec44} изучается устойчивость нейронной сети
\eqref{main2chapter4} с ограниченным числом нейронов при разрыве одной из связей между нейронами и 
переходе от кольцевой сети к линейной. Проводится численное исследование
изменения областей устойчивости и неустойчивости сети  из
шести нейронов при постепенном уменьшении связи между первым и шестым нейронами.
Приводится серия рисунков, иллюстрирующая переход от области устойчивости,
характерной для кольцевой нейронной сети~\eqref{common:main2}, к области,
ограниченной гиперболами и свойственной для нейронных сетей линейной
конфигурации.


 В заключении
суммируются все полученные в диссертации результаты.

В {\bfseries приложениях} приводятся исходные коды программных продуктов, 
разработанных в~программном пакете \Eng MATLAB~7.11.0~(R2010b) \Russian и описанных в
разделах \ref{sec22}, \ref{sec32} и \ref{sec41} диссертации.
Код программы <<Анализ устойчивости>> находится в приложении А, код программы
<<Устойчивость нейронных  сетей>> --- в приложении Б, код программы 
<<Построение  областей устойчивости круговых нейронных сетей>>~--- в приложении
В. Приложение Г содержит общие части исходных кодов всех трех программ: 
во избежание повторений те функции,
которые используются в работе всех программ (функция построения конуса
устойчивости, функция нахождения значений запаздывания, обеспечивающих
устойчивость исследуемого уравнения при фиксированных коэффициентах, и др.),
вынесены в отдельное приложение.

}
